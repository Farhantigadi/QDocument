import { useMemo } from 'react';
import { Activity, Database, FileStack, HardDrive, ShieldCheck, Users } from 'lucide-react';
import { useGetAdminOverview, useGetSession, useListUsers } from '@workspace/api-client-react';
import { formatBytes, QueryState } from '@/components/vault-ui';

export default function Admin() {
  const session = useGetSession();
  const overview = useGetAdminOverview();
  const users = useListUsers();
  const sortedUsers = useMemo(() => [...(users.data ?? [])].sort((a, b) => b.storageUsedBytes - a.storageUsedBytes), [users.data]);
  const metrics = overview.data ? [
    { label: 'People with access', value: overview.data.userCount, icon: Users, testId: 'users' },
    { label: 'Documents held', value: overview.data.documentCount, icon: FileStack, testId: 'documents' },
    { label: 'Storage in use', value: formatBytes(overview.data.storageUsedBytes), icon: HardDrive, testId: 'storage' },
    { label: 'Audit events', value: overview.data.auditEventCount, icon: Activity, testId: 'events' },
  ] : [];

  return (
    <div className="vault-page mx-auto max-w-[1440px] px-5 py-8 sm:px-8 lg:px-10 lg:py-11" data-testid="page-admin">
      {session.data?.user?.role !== 'ADMIN' && <div className="vault-card rounded-2xl p-10 text-center" data-testid="state-admin-forbidden"><ShieldCheck className="mx-auto h-8 w-8 text-accent" /><h1 className="display mt-4 text-3xl">This room is private.</h1><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-muted-foreground">Administrator tools are only available to Haven administrators.</p></div>}
      {session.data?.user?.role === 'ADMIN' && <>
      <div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow text-accent">For the people who keep Haven steady</p><h1 className="display mt-2 text-4xl sm:text-5xl" data-testid="heading-admin">Admin overview<span className="text-accent">.</span></h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">A privacy-safe view of account health. No document contents, ever.</p></div><div className="flex items-center gap-2 rounded-full bg-secondary px-3 py-2 text-xs font-semibold text-primary"><ShieldCheck className="h-3.5 w-3.5" /> Metadata only</div></div>
      <QueryState loading={overview.isLoading} error={overview.error} onRetry={() => void overview.refetch()} empty={<div className="mt-8" data-testid="empty-admin-overview"><div className="vault-card rounded-2xl p-10 text-center"><Database className="mx-auto h-8 w-8 text-accent" /><p className="mt-4 font-semibold">Admin metrics are not connected</p><p className="mx-auto mt-2 max-w-sm text-sm text-muted-foreground">Overview data will settle here when the administrator service is available.</p></div></div>} />
      {overview.data && <><div className="vault-stagger mt-9 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ label, value, icon: Icon, testId }) => <div className="vault-card rounded-2xl p-5" key={testId} data-testid={`admin-metric-${testId}`}><div className="flex items-center justify-between"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary"><Icon className="h-4 w-4" /></span><span className="eyebrow text-muted-foreground">Live</span></div><p className="mt-7 text-3xl font-semibold tracking-tight" data-testid={`admin-value-${testId}`}>{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></div>)}</div><section className="mt-10"><div className="mb-5 flex items-center justify-between"><div><p className="eyebrow text-accent">Account health</p><h2 className="display mt-1 text-3xl">People and their space</h2></div><span className="text-xs text-muted-foreground">Status & quota only</span></div><QueryState loading={users.isLoading} error={users.error} onRetry={() => void users.refetch()} empty={<div className="vault-card rounded-2xl p-9 text-center text-sm text-muted-foreground" data-testid="empty-admin-users">No user accounts to show.</div>} />{sortedUsers.length > 0 && <div className="overflow-x-auto rounded-2xl border border-border bg-card shadow-[var(--shadow-sm)]"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-border bg-muted/45 text-xs text-muted-foreground"><tr><th className="px-5 py-4 font-medium">Account</th><th className="px-5 py-4 font-medium">Status</th><th className="px-5 py-4 font-medium">Documents</th><th className="px-5 py-4 font-medium">Storage</th><th className="px-5 py-4 font-medium">Usage</th></tr></thead><tbody className="divide-y divide-border">{sortedUsers.map((user) => { const percent = Math.min(100, (user.storageUsedBytes / Math.max(user.storageLimitBytes, 1)) * 100); return <tr className="transition-colors hover:bg-muted/30" key={user.id} data-testid={`row-user-${user.id}`}><td className="px-5 py-4"><p className="font-semibold" data-testid={`text-user-name-${user.id}`}>{user.name}</p><p className="mt-0.5 text-xs text-muted-foreground">{user.email}</p></td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-2.5 py-1 text-[11px] font-semibold ${user.status === 'ACTIVE' ? 'bg-secondary text-primary' : 'bg-destructive/10 text-destructive'}`} data-testid={`status-user-${user.id}`}>{user.status.toLowerCase()}</span></td><td className="px-5 py-4 mono text-xs">{user.documentCount}</td><td className="px-5 py-4 mono text-xs">{formatBytes(user.storageUsedBytes)} <span className="text-muted-foreground">/ {formatBytes(user.storageLimitBytes)}</span></td><td className="w-44 px-5 py-4"><div className="flex items-center gap-3"><div className="h-1.5 flex-1 rounded-full bg-muted"><div className="h-full rounded-full bg-accent" style={{ width: `${percent}%` }} /></div><span className="mono w-8 text-right text-[11px] text-muted-foreground">{Math.round(percent)}%</span></div></td></tr>; })}</tbody></table></div>}</section></>}
      </>}
    </div>
  );
}