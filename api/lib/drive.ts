// Google Drive integration — deferred (Coming Soon)
// All functions below are commented out pending Drive API setup.

/*
import { google } from "googleapis";
import { Readable } from "stream";

export type DriveDocument = { ... };
export type VaultIndex = { ... };

export function getDriveClient(accessToken: string) { ... }
export async function getOrCreateVaultFolder(accessToken: string): Promise<string> { ... }
export async function readIndex(accessToken: string, folderId: string): Promise<VaultIndex> { ... }
export async function writeIndex(accessToken: string, folderId: string, index: VaultIndex): Promise<void> { ... }
export async function uploadFileToDrive(...): Promise<{ driveFileId: string }> { ... }
export async function deleteFileFromDrive(accessToken: string, driveFileId: string): Promise<void> { ... }
*/

export {};
